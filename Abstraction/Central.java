package Abstraction;

public class Central extends Master{ //when we inherit the superclass into subclass then subclass can also have a corona
    @Override
    public void display()
    {
        System.out.println("Display Method");
    }
    // because of that abstract class for that we have to provide a implementation for
}
